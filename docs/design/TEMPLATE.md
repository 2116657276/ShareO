# 设计文档: <主线名称>

> 更新时间: YYYY-MM-DD | 状态: 草稿 | 评审中 | 定稿 | 已实现

## 1. 背景与目标

要解决什么问题，做完后用户能干什么。2~4 行。

## 2. 非目标

明确**不做**什么（范围控制的关键，防止 Phase 膨胀）。

## 3. 总体方案

一段话 + 一张图（数据流/时序）。与 architecture.md 的对应关系。

## 4. 数据模型

新表/改表的 DDL 草案、索引设计、与现有表的关系。对应 migrations 编号。

## 5. 接口设计

| 方法 | 路径 | 入参 | 出参 | 权限 |
|------|------|------|------|------|

WebSocket / Streams 消息格式也在此列出。

## 6. 关键流程

正常路径 + 至少两个异常路径（组件挂了怎么办、重复消息怎么办）。

## 7. 风险与权衡

已知的坑、性能预估、放弃了什么。

## 8. 评测与测试方案

- 功能验收清单（可演示标准，对齐 roadmap 的验收列）
- 涉及 AI 的：评测集与指标（对齐 docs/eval/ 规范）
- 自动化测试范围
